# PostgreSQL with optimizations
FROM postgres:17

# Add any custom initialization scripts if needed
# COPY docker_configs/postgres/init-scripts/ /docker-entrypoint-initdb.d/

# Use default PostgreSQL setup with optimizations
USER postgres

